<label class="text-field {{ fieldClass }}">
  <span class="text-field__label">{{ label }}</span>
  <input name="{{ name }}" type="{{ inputType }}" class="text-field__input" placeholder="Enter text" />
  <span class="text-field__error {{ errorClass }}">{{ errorText }}</span>
</label>
