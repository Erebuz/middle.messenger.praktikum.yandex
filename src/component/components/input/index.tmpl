<input name="{{ name }}"
       {{ id }}
       type="{{ inputType }}"
       {{ value }}
       class="text-field__input"
       placeholder="{{ placeholder }}"
       {{ pattern }}
       {{ required }}
/>
