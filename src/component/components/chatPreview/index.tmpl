<div class="chat-preview">
  <div class="chat-preview__img">{{ avatar }}</div>

  <div class="chat-preview__name">{{ title }}</div>

  <div class="chat-preview__message">{{ last_message }}</div>

  <div class="chat-preview__time">{{ created_by }}</div>

  <div class="chat-preview__count">{{ unread_count }}</div>
</div>
